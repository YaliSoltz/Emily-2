NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
# Optional. Only used by the admin panel's "generate 360" button, to bill
# Hugging Face ZeroGPU time to your account instead of the shared anonymous
# per-IP pool. The feature works without it, just with a much smaller quota.
HF_TOKEN=
